{
  "name": "agents2024",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "parcel build src/chat_app.ts --dist-dir dist",
    "watch-ts": "parcel watch src/chat_app.ts --dist-dir dist",
    "start-server": "cd server && python chat_app.py",
    "dev": "concurrently \"npm run watch-ts\" \"npm run start-server\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "concurrently": "^8.0.0",
    "nodemon": "^3.0.0",
    "parcel": "^2.0.0",
    "ts-node": "^10.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "marked": "^15.0.0"
  }
}
